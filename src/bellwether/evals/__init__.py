"""C1 评测运行器（RFC-003）：校验 report.json 的 IR，四维判分。

四维（RFC-003 §0）：事实性（程序化硬判，B 层零容忍）、完整性（分报告类型机械
清单）、合规（M2 规则层）、推理质量（LLM 评审 + rubric，n 次均值 ± 95% CI）。
分层确定性（§3）：程序化维度对同一 report.json 重复评分逐位一致；只有推理
质量维带随机性并以置信区间呈现。
"""
