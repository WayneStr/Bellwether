# Bellwether 企业级路线图 v2.1（评审修订版）

> **状态：规划定稿 —— 已完成两路独立评审（deep-reasoner + Codex）并综合修订；尚未开始实施。** 最后更新 2026-07-16。
> 相关：[README](README.md)（当前功能）· [DESIGN](DESIGN.md)（v0 蓝图）· [评审原文](docs/reviews/)（两份独立评审全文）。

---

## 0. v2 → v2.1 变更摘要（先读这一节）

v2 经 deep-reasoner 与 Codex **并行独立对抗评审**（互不可见，原文存 `docs/reviews/`）。两边独立得出高度一致的结论；共识全部采纳，两处分歧由维护者裁决（§9 决策 13/14）。核心变更：

1. **战略承诺重述**（两评审共识 + 裁决 13）：对外可验证承诺改为「**最可审计的开源 US/CN/HK 股票研究 agent**」；「全球顶尖」保留为内部北极星愿景；公开基准按**免费源档/专业源档**两档诚实披露。
2. **新增 A0 每日快照任务，立刻上线**（deep-reasoner 的「时间物理」论证）：新闻等免费源无历史存档、永远无法回填——日历时间是唯一买不回来的资源。A0 是全计划最高优先级。
3. **三处依赖外科手术**（两评审共识，均为阻断级）：A7a(EDGAR)→M3 置于 B7 之前；E3 许可审计→M1（评测 cassette 若含原始行情数据入公开仓库即构成违规再分发）；E1 最小版 + MCP 安全测试 = **M5 GA 门禁**。
4. **证据 IR 成为地基**（Codex 主张 + deep-reasoner 证据 ID 方案合流）：「数字零臆造」从事后文本核查升级为**构造性保证**——报告数字只能由 Claim/Evidence IR 渲染，LLM 只能引用证据 ID。IR/report schema/provenance manifest 提前到 M2 与评测同步落地。
5. **评测体系可运营化**（共识）：分层确定性（冻结输入确定 + 程序化核查确定 + LLM 分数均值±置信区间）；噪声感知门禁 + 分维度地板；钉死 baseline-of-record；评测分层（PR 抽样 20 例/nightly 半量/发布全量）；prompt caching 提前至 M2。
6. **B2 对抗审查改为受控实验**（共识）：验收从「改 20%」（Goodhart 反向激励）改为**播种错误检出率 + 盲评净增益**，并设退出条件——无显著净增益则保持单 agent + 确定性 verifier。
7. **PIT 双时间语义**（Codex）：有效时间 vs 获知时间；仅带发布时间的源可称 PIT，其余标注「历史回放」；存原始价格+公司行动、复权现算；快照 schema 迁移策略前置。
8. **安全风险大补**（共识）：提示注入/引用投毒、插件供应链、MCP 资源耗尽与数据外传、黄金集过拟合、模型漂移全部入册并带缓解。
9. **工期重估**：两评审独立估出同一区间 **35–55 工程周**（原 19–27 低估约 2 倍）；经温和砍减后计划按 **约 30–48 工程周** 排布。
10. **温和版砍减**（裁决 14）：B4 因子筛选、A8 宏观、A7b 巨潮/HKEX 摄取 → v1.1 backlog；OTel → 可选 extra；PDF → stretch；文档站英文为主；TCK 最小化。**保留** B5 高级形态与 B6 事件分类（用户可见的分析能力）。
11. 修复两处与既定裁决的自相矛盾（Codex 抓出）：删除 A7 电话会纪要残留；F5「对比表」改为能力矩阵。

### 定位与非目标（不变部分）

- 形态：开源 Python CLI/库；免费数据源默认 + 专业源走插件。
- ❌ 永久非目标：实盘下单/自动交易/个性化买卖建议（合规红线）。
- ❌ 本轮不做：Web 多租户 SaaS、认证计费；REST API/TUI 留决策门。

---

## 1. 愿景与可验证承诺

**对外可验证承诺（v1.0）**：**最可审计的开源 US/CN/HK 股票研究 agent** ——
每个数字可溯源（构造性 100%）、每份报告可复现（provenance 回放）、评测方法公开且可重复（钉死基线 + 冻结输入）。

**内部北极星愿景（长期）**：全球顶尖的股票投资分析 agent。对外不以此作宣称——已裁决不点名对比同类项目，故相对排名不可证；用可审计性建立信任，让口碑完成剩下的论证。

**长期护城河**（评审共识修正后）：不是插件生态（那只是及格线），而是——
1. **从 A0 开始积累、任何后来者都无法回填的 PIT 语料与评测资产**；
2. 证据链（IR/溯源/回放）的架构级保证；
3. 跨市场（US/CN/HK）数据语义的正确处理（复权/币种/口径/日历）。

**分档诚实原则**：免费源默认体验与专业源体验存在客观差距；公开基准分按「免费源档/专业源档」两档披露，杜绝「专业数据跑分、免费用户复现不出」的质疑。

### 「可审计」的可度量定义

| 维度 | 度量 | 目标水位 |
|------|------|---------|
| 溯源 | 受控 claim 范围内 provenance 完整率（构造性） | =100%，任何版本不得回退（硬门禁） |
| 可复现 | 报告可回放率（trace playback）/ 评测输入冻结率 | 100% / 100% |
| 评测 | paired 评测 vs 钉死基线（分维度） | 事实/合规维度零容忍；综合分详见 §6 |
| 可靠 | 出报率 / **全量数据率**（分列，防刷分） | ≥99% / 单列跟踪+告警 |
| 合规 | 红队违规（轮换样本集） | =0（门禁） |

---

## 2. 参照系与差异化

| 参照 | 特点 | 借鉴 / 区别 |
|------|------|------------|
| OpenBB | 开源投研数据平台 | 借鉴数据源生态；我们以可审计分析为核心而非数据终端 |
| TradingAgents（研究） | 多智能体辩论 | 借鉴角色化对抗；区别：证据 IR + 受控实验验证增益才扩张 |
| FinGPT / ai-hedge-fund | 金融 LLM / 教学 agent | 我们不训模型、不做实盘，聚焦可验证研究报告 |
| Bloomberg/Wind | 数据深度天花板 | 不对标采购；插件让用户自带专业数据 |

对外呈现用**能力矩阵 + 可复现基准页**（免费/专业两档，只对比钉死的「裸 LLM+搜索」基线），不做点名对比表（决策 10/13）。

---

## 3. 目标架构 v2.1

```
┌─────────────────────────────────────────────────────────────────────┐
│ 接口层    CLI (typer) │ 报告导出 md/HTML(PDF=stretch) │ MCP(M5,带安全门禁) │
├─────────────────────────────────────────────────────────────────────┤
│ 编排层    默认: analyze 单 agent 快路径                                │
│           --deep: 多智能体全编排（受控实验：无盲评净增益则不扩张）      │
│           模型路由 · Token 预算硬上限 · 会话持久化与 trace 回放        │
├─────────────────────────────────────────────────────────────────────┤
│ ★证据层   Claim/Evidence IR：值·单位·币种·期间·as_of·source_id·       │
│           计算链·置信状态 —— 报告数字只能由 IR 渲染（构造性溯源）      │
│           machine-readable report schema + provenance manifest       │
├─────────────────────────────────────────────────────────────────────┤
│ 分析引擎（确定性、可单测）                                            │
│   估值: DCF+敏感性·反向DCF·可比自动选取 │ 技术: 多周期·形态·状态       │
│   事件: 去重·分类·重要性 │ 组合: 相关/回撤/敞口                        │
├─────────────────────────────────────────────────────────────────────┤
│ 知识检索  A7a: EDGAR 全文摄取(M3) → 分块索引 → 带引用 RAG(B7)          │
│           摄取内容指令隔离(防提示注入) · 自由行业分类(非GICS)           │
├─────────────────────────────────────────────────────────────────────┤
│ 数据平台  A0 每日原始快照(立即) → Provider 插件(async+限流声明)        │
│           → 质量门 → 双时间 PIT 存储(DuckDB+Parquet，原始价+公司行动)  │
│           → 分层缓存 │ 符号主表·交易日历·覆盖矩阵门禁                  │
├─────────────────────────────────────────────────────────────────────┤
│ 评测体系  C2a 冻结 cassette(M2) / C2b PIT 多时点(A0 积累)              │
│           C1 checks IR · 噪声感知门禁 · 钉死基线 · 分层运行            │
├─────────────────────────────────────────────────────────────────────┤
│ 横切      structlog+本地trace(OTel=可选) · token/成本记账+硬预算       │
│           重试/熔断/降级链(含LLM侧) · per-provider 限流                │
│           密钥keyring · 合规护栏(GA门禁) · 分析溯源                    │
└─────────────────────────────────────────────────────────────────────┘
```

**RFC 先行**（动代码前评审通过，必答题为评审共识）：
- `RFC-001 编排与档位`：每阶段预算绝对上限与降级路径（对抗轮超时→单审稿人）；受控实验设计与退出条件；trace 回放格式。
- `RFC-002 数据平台`：双时间语义（有效/获知）；PIT vs「历史回放」的标注边界；**存原始价+公司行动、复权现算**（防复权重写制造假前视）；A0 快照的 schema 迁移策略（M0 起积累的资产必须活过 M3 演进）。
- `RFC-003 证据 IR 与评测`：IR 字段定稿；report schema；分层确定性定义；噪声带测定方法；评测分层与成本预算。

---

## 4. 工作流详表（v2.1 修订）

图例：⬆ 提前 · ⬇ 延后 · ★ 新增 · ✂ 移入 v1.1（§10）

### WS-A · 数据平台

| ID | 任务 | 验收标准 |
|----|------|---------|
| ★A0 | **每日原始快照任务（立即上线，先于一切）**：黄金集 90 标的的行情/基本面/新闻原始 API 响应每日落盘（含 as_of、来源、许可元数据、哈希清单）；同批实现每日 live smoke（3 市场×3 标的，非阻塞告警） | 连续 7 天无人工干预稳定运行；快照带版本化 manifest；smoke 失败有告警记录 |
| A1 | Provider 协议 v2：async 化、能力声明（市场/数据类型/粒度/PIT 支持/**速率限制**）、健康检查；M1 先落 sync-to-async 边界（取消语义/线程池上限/to_thread 不可中止说明） | 协议文档+运行时校验；现有 3 源通过；D2 的重试熔断建在该边界上不需 M3 重做 |
| A2 | 插件系统：entry-points 发现 + **最小 TCK**（官方示例插件 + 可导入 pytest fixtures；测单位/币种/时区/复权/错误语义）；TCK 先于 A6 | 外部 demo 插件 pip 装上即被发现并通过 TCK |
| A3 | **双时间 PIT 存储**：DuckDB+Parquet；有效时间 vs 获知时间两轴；仅带发布/可得时间的源标 PIT，其余标「历史回放」；存原始价格+公司行动、复权现算；快照 schema 迁移策略；M3 时把 C2a fixture 与 A0 积累迁入 | 抽检零前视；复权重算与源一致；A0 早期快照完整迁移 |
| A4 | 数据质量框架：OHLC 合理性/复权正确性/缺口检测、跨源对账、质量分、不合格隔离 | 注入脏数据被拦截并告警 |
| A5 | 符号主表与市场基础设施：代码变更/退市/三市场交易日历；**自由行业分类**（yfinance sector + 申万/中证公开层级，文档明示非官方 GICS） | 三市场日历正确；退市查询无幸存者偏差 |
| ⬆A7a | **SEC EDGAR 全文摄取（M3，B7 的前置）**：10-K/Q/8-K 不可变原文 + 页/段锚点 + 索引；摄取内容指令隔离（防注入） | 指定美股能拉取最近 N 份文件并可锚点引用 |
| ✂A7b | 巨潮/HKEX 文件摄取 → v1.1（同时是插件生态示范题材） | — |
| A6 | 专业源适配插件（M5，晚于 TCK）：美股 Polygon/FMP（开工前专项对比，决策 7）；A股 Tushare Pro 档位同期定 | 装插件+配 key 无缝走专业源 |
| ✂A8 | 宏观数据 → v1.1 | — |
| ★A9 | **市场×模块覆盖矩阵门禁**：数据缺失时禁止生成该维度结论（宁缺毋滥），缺失率单列上报、不计入「降级成功」；本项含**港股基本面源补全**（M3） | HK 估值可用或该维度显式静默；矩阵进报告元数据 |

### WS-B · 分析引擎与 Agent 智能

| ID | 任务 | 验收标准 |
|----|------|---------|
| ★B0 | **Claim/Evidence IR + report schema + provenance manifest（M2，评测地基）**：IR 字段=值/单位/币种/期间/as_of/source_id/计算链/置信状态；派生值（如同比%）一律代码预算入 IR；**报告数字只能以证据 ID 引用、渲染层替换**；LLM 无法凭空引入数字 | 构造性溯源 100% 机械可验；C1 直接校验 IR |
| B1 | 多智能体编排 v2（按 RFC-001）：角色图、结构化消息、每阶段预算/超时、会话持久化与 trace 回放；默认 `analyze` 单 agent，`--deep` 全编排 | deep 分析可完整回放；预算超限优雅截断 |
| B2 | 对抗性审查 = **受控实验**：验收用①播种错误检出率 ≥X% 且误挑战率 ≤Y%（错误样本从黄金集构造）②盲评三指标（错误修正精度/无根据断言下降/遗漏补回率）净增益显著；**退出条件：无显著净增益 → 保持单 agent+确定性 verifier，不扩张角色图** | 实验报告归档；扩张与否有数据依据 |
| B3 | 估值引擎：轻量三表、DCF+WACC+敏感性矩阵、可比自动选取（自由分类+规模）、反向 DCF | 纯函数+单测+手算对拍；敏感性进报告 |
| B5 | 技术引擎 v2（保留，裁决 14）：多周期共振、支撑阻力、量价形态（规则化+已知案例测试）、市场状态识别 | 每形态有案例测试；不输出买卖指令 |
| B6 | 事件引擎 v2（保留，裁决 14）：去重、事件分类、重要性评分、实体消歧；标注集自建（工作量入 M4 估算） | 标注样本上分类准确率 ≥85% |
| B7 | 带引用 RAG（M4，范围=EDGAR/A7a）：每条文件类论断**构造性 100%** 带锚点引用（无证据则不输出该论断）；对抗性文档评测用例（防引用投毒） | 引用可点击到原文锚点；注入样例不改变结论 |
| B8 | 报告引擎 v2：模板、证据表（由 IR 驱动）、图表、情景块、置信度/数据新鲜度横幅、**中英双语报告**（含术语/单位/引用一致性评测）；★可选 **ex-ante claim schema**（事件/期限/概率/可验证结果，非投资建议措辞）供 C4 用；导出 md/HTML（PDF=stretch） | 双语一致性通过评测；ex-ante claims 机器可读 |
| B9 | Prompt 资产管理：版本化+变更记录+评测门禁；**模型版本变更 = 强制重测基线** | 劣化 PR 被拦截（演练）；换模型有重定基线记录 |
| B10 | 成本优化：路由策略精修、上下文压缩（prompt caching 已提前至 M2） | quick 路径成本 M6 较 M2 基线 ≥-30% |
| ✂B4 | 因子库+筛选器 → v1.1 | — |

### WS-C · 评测与回测

| ID | 任务 | 验收标准 |
|----|------|---------|
| C1 | 评测运行器（M2）：**校验 IR 而非自由文本**——事实性（IR 溯源，程序化硬判）、完整性（分报告类型清单）、推理质量（LLM 评审+rubric）、合规；**分层确定性**：冻结输入与程序化分数确定，LLM 分数=n 次均值±置信区间；quick 与 deep 两档都测 | 一条命令出分数报告；程序化维度可复现一致 |
| C2a | **当前时点冻结黄金集（M2，无 A3 依赖）**：30 标的×3 市场，原始 tool cassette + 哈希清单 + as_of/来源/许可元数据 + 版本化 manifest；存放策略遵守 E3（私有存储/本地再生成/仅派生指标三选一） | 冻结输入下程序化分数完全可复现；许可合规 |
| C2b | 多时点 PIT 黄金集（M4 起成形）：依赖 A0 积累 + A3 迁移 | 时点数随日历时间增长；无回填造假 |
| C3 | 评测门禁（噪声感知版）：先实测同版本 k 次噪声带；**paired 同题对比**；回退超噪声带才阻断；**分维度地板**（事实/合规零容忍；推理质量可带签字豁免）；**评测分层**：PR 固定抽样 20 例 / nightly 半量 / 发布前全量 | 门禁演练通过；月度 flaky 率有记录且收敛 |
| C4 | 校准回测（范围修正）：仅对 B8 的 ex-ante claims 用**预先锁定**的 Brier/log-loss 评分；价格/财务陈述用回填 PIT，新闻类陈述用 A0 快照；M6 交付**首份校准报告（明示窗口局限）**，完整版随日历时间滚动 | 评分方法先于数据锁定；报告披露窗口与样本量 |
| C5 | 公开基准：**钉死 baseline-of-record**（模型版本+冻结搜索 cassette+提示词+预算+运行日期）；**免费源档/专业源档两档披露**；≥1 个非 Anthropic 评审模型；对最新模型只报告不门禁 | 基准页可复现；两档分数并列 |
| C6 | 人工评审环：抽样专家评审 + **人工-LLM 评审一致性校准**（相关性达标 LLM 分数才可代表质量） | 每里程碑一轮记录；一致性系数公开 |

### WS-D · 工程卓越

| ID | 任务 | 验收标准 |
|----|------|---------|
| D1 | 工程地基：src layout、ruff、mypy（**新代码 strict，数据适配层放宽**）、pre-commit、CI 矩阵 | CI 绿；策略写入 CONTRIBUTING |
| D2 | 可靠性：异常层级、清裸 except、tenacity 重试、显式超时、数据源降级链、熔断；★**LLM 提供方故障的降级行为**（重试→换模型档→明示失败） | 故障注入覆盖数据源与 LLM 两侧 |
| D3 | 可观测：structlog+请求上下文、**本地结构化 trace 文件**（OTel 降为可选 extra，MCP/API 转正时再评审）、token/成本/时延记账（定价表内置+可覆盖）、`--verbose/--json/--quiet`、成本硬预算 | 每次分析出成本摘要；trace 支撑 E4 回放 |
| D4 | 性能：async 并发取数、**per-provider 限流器（令牌桶/信号量，读 A1 声明）**、agent 多 tool 并发、分层缓存（TTL 按数据类型）、基准脚本 | 并发不触发封禁；基准前后对比 |
| D5 | 测试与发布：集成（vcrpy）、E2E（CliRunner+mock LLM）、指标 property-based、覆盖率 ≥85% 门禁、Docker 多阶段、PyPI OIDC、pip-audit/dependabot+SBOM | CI 全绿；tag→PyPI 全自动 |
| D6 | 安全工程：keyring+env、日志脱敏、SECURITY.md | 密钥扫描零泄漏 |

### WS-E · 投研域合规与安全

| ID | 任务 | 验收标准 |
|----|------|---------|
| ⬆E3 | **数据许可审计（M1，先于任何 cassette 产出）**：yfinance/akshare 条款、再分发边界、**黄金集存放策略**（私有/本地再生成/仅派生）、自由行业分类替代 GICS 的合规确认、专业源许可移交插件用户 | `docs/data-licensing.md`；C2a 存放策略由此裁决 |
| E1 | 合规护栏分两级：**最小版（M5 GA 门禁）**=规则层建议检测+全导出面免责+50 条红队；完整版（M6）=双层分类器+**每版本轮换**的红队样本集+分法域措辞 | GA 前红队零违规；样本集轮换有记录 |
| E2 | 模型风险管理文档：失败模式、幻觉控制、人在环内、事故分级 | `docs/model-risk.md` 评审通过 |
| E4 | 分析溯源：provenance 包=输入哈希+数据快照 ID+模型/prompt 版本+完整 tool 调用记录；**「重放」显式定义为 trace playback（回放已录制的 LLM 输出）** | 任意历史报告可回放出一致渲染结果 |
| ★E5 | **提示注入与摄取安全**：A7a/B7 摄取内容指令隔离（quarantine framing）、黄金集加入对抗性文档用例；**MCP 安全（M5 GA 门禁）**：输入隔离、资源限制、只读默认、限流 | 注入样例不能改变结论或触发越权动作 |

### WS-F · 交付、文档与生态

| ID | 任务 | 验收标准 |
|----|------|---------|
| F1 | 文档站：快速上手/CLI/架构/插件开发/评测方法/贡献；**英文为主**，中文 AI 辅助镜像（接受滞后）；API 参考自动生成 | 新人 30 分钟跑通（实测） |
| F2 | 社区基建：CONTRIBUTING/CoC/SECURITY/模板/good-first-issue/仓库级 CLAUDE.md | 外部首个 PR 可自助 |
| F3 | 发布工程：semver、月度 minor、CHANGELOG、升级指南 | 连续两版按节奏 |
| F4 | 分发：PyPI `bellwether-agent`（CLI 仍为 `bellwether`）、Docker；Homebrew=stretch | `pip install bellwether-agent` 可用 |
| F5 | 展示面：样例报告画廊、**能力矩阵** + 可复现基准页（两档披露；不做点名对比表） | 基准页结果可复现 |
| F6 | MCP server（M5，随 GA）：过 E5 安全门禁；REST API/TUI 仍为后续决策门 | 宿主内可调用分析工具且通过安全测试 |

---

## 5. 里程碑（M0–M6，v2.1 重排）

> 工期为「1 主力+AI 协作」的粗估区间，**含温和砍减**；两评审对原范围的独立重估均为 35–55 周，本表合计 **约 30–48 周**。⚠️ 两类日历时间不可压缩：A0 的 PIT 积累、社区/专家评审响应。

| 里程碑 | 范围（任务 ID） | 退出标准（可验证） | 粗估 |
|--------|----------------|--------------------|------|
| **M0 治理与快照起跑** | **A0（最高优先）**、RFC-001/002/003 立项、ADR 建仓（补录 §9 全部裁决）、冻结 v0.x、定性基线锚点（仅作参照） | A0 连续 7 天稳定；三 RFC 过评审；v0.x tag | 1–2 周 |
| **M1 地基+可靠性+许可** | D1、D2、D6、**E3**、A1(sync-to-async 边界)、E4(最小)、B0 的 IR 设计定稿（RFC-003） | CI 矩阵绿+覆盖率门禁；故障注入通过（含 LLM 侧）；`data-licensing.md` 裁决 cassette 策略；IR spec 冻结 | 3–5 周 |
| **M2 证据层+评测闭环** ⭐ | **B0 落地**、C1、C2a、C3（噪声感知+分层）、B9、D3、prompt caching 启用、**正式基线重测**（冻结 v0.x + 钉死裸 LLM 基线） | 构造性溯源机械可验；劣化 PR 被门禁拦截（演练）；每次分析出成本摘要；基线分数归档 | 4–6 周 |
| **M3 数据平台 v1** | A1(async 全量)、A3(双时间 PIT+迁移 A0/C2a)、A4、A5、**A7a(EDGAR)**、**A9(覆盖矩阵+HK 基本面)**、D4(并发+限流) | 抽检零前视；HK 估值可用或显式静默；并发不封禁；EDGAR 文件可锚点引用 | 5–8 周 |
| **M4 智能升级 v1** ⭐ | B1、B2(受控实验)、B3、B5、B6、B7(EDGAR 范围)、B8(含 ex-ante schema)、C2b 起步 | **paired 评测较 M2 正式基线提升 ≥30%（分维度地板，quick+deep 两档）**；B2 实验报告决定扩张与否；引用构造性 100% | 6–10 周 |
| **M5 生态与 GA** | A2(TCK 先行)、A6、D5、F1–F4、F6(MCP)；**GA 门禁 = E1 最小版 + E5 的 MCP 安全测试 + E3 结论落地** | demo 插件过 TCK；≥1 专业源可用；`bellwether-agent` v1.0 GA；红队（50 条）零违规；MCP 安全测试通过 | 5–8 周 |
| **M6 可信度冲刺** | C4(首份校准报告)、C5(公开基准两档)、C6(一致性校准)、E1 完整版、E2、B10、F5 | 基准页可复现发布；校准报告明示窗口局限；轮换红队零违规；quick 成本 -30% | 5–8 周 |

**排序逻辑（v2.1 修正后）**：A0 抢时间 → 许可先于数据资产产出 → 证据 IR 先于评测 → 评测先于智能升级 → 文档摄取先于引用 RAG → 合规先于 GA。评测仍在数据平台之前（M2 vs M3），但靠 C2a「冻结 cassette 不依赖 A3」解除了原依赖断裂。

---

## 6. KPI 与质量门禁（CI 强制项加粗）

| 类别 | 指标 | 门禁/目标 |
|------|------|----------|
| 质量 | **构造性溯源完整率（受控 claim 范围）** | **=100%，机械可验，任何版本不得回退** |
| 质量 | **评测门禁（paired vs 钉死基线）** | **事实性/合规维度零容忍；综合分回退超实测噪声带则阻断；分维度地板** |
| 质量 | 文件类论断引用覆盖 | =100%（构造性：无证据不输出该论断） |
| 可靠 | 出报率 / **全量数据率**（分列） | ≥99% / 单列跟踪，降级率上升告警；缺维度=静默不计成功 |
| 可靠 | p95 时延（基准环境定义于评测文档） | quick ≤60s；deep ≤5min |
| 成本 | 单次 quick 分析成本 | M6 较 M2 基线 ≥-30%；deep 单列跟踪（不设降幅门禁） |
| 工程 | **测试覆盖率** / CI 时长 | **核心包 ≥85%** / ≤10min |
| 合规 | **红队违规（每版本轮换样本）** | **=0** |
| 生态 | 外部插件/贡献者（**北极星，非门禁**） | 跟踪；M6 退出标准改为可控项：官方示例插件 ≥2 过 TCK、插件文档 30 分钟实测跑通 |

---

## 7. 风险登记册（v2.1 扩充至 12 项）

| # | 风险 | 概率 | 影响 | 缓解 |
|---|------|------|------|------|
| 1 | 免费源限流/封锁/变更 | 高 | 高 | A0 live smoke 实时预警；A1 限流声明+D4 主动限流；D2 降级链；A6 专业源出路 |
| 2 | LLM 成本失控 | 高 | 中 | C3 评测分层；prompt caching(M2)；D3 硬预算；C2 冻结输入零数据成本 |
| 3 | 评审偏差与 judge circularity | 中 | 高 | 事实性程序化硬判；C5 非 Anthropic 评审；C6 人工一致性校准 |
| 4 | 范围蔓延（「顶尖」无上界） | 高 | 高 | 承诺重述（决策 13）；里程碑退出标准；v1.1 backlog 纪律（新想法不插队） |
| 5 | 被当投顾使用 | 中 | 高 | E1 两级护栏（最小版=GA 门禁）；轮换红队；永久非目标声明 |
| 6 | 单人维护 bus factor | 高 | 中 | ADR/RFC/评审留痕；F2 社区；AI 协作友好仓库 |
| 7 | 多智能体复杂度与成本 | 中 | 中 | B2 受控实验+退出条件；RFC-001 预算上限与降级路径；档位隔离 |
| 8 | 数据许可/再分发（含黄金集 cassette、GICS） | 高 | 高 | **E3 提前 M1**；cassette 三选一存放策略；自由分类替代 GICS |
| 9 | ★提示注入/引用投毒（A7a/B7/MCP） | 中 | 高 | E5 摄取隔离；对抗性文档评测用例；MCP 输入隔离 |
| 10 | ★插件供应链（entry-points 任意代码） | 中 | 高 | 未知插件不自动启用（显式配置）；TCK+签名/审核指引；文档警示 |
| 11 | ★MCP 资源耗尽/数据外传 | 中 | 中 | E5：只读默认、限流、资源上限；GA 门禁测试 |
| 12 | ★黄金集过拟合与模型/搜索漂移 | 中 | 中 | 红队与评测样本轮换；钉死 baseline-of-record；模型换代强制重定基线（B9） |

---

## 8. 治理与工作方式

- **RFC 先行**：三大子系统（RFC-001/002/003，含 §3 必答题）过评审再动代码。
- **ADR 记录**：`docs/adr/`；本文 §9 全部裁决在 M0 补录正式 ADR。
- **评审留痕**：重大计划变更须经独立评审，原文归档 `docs/reviews/`（本轮两份已归档）。
- **评测先行铁律（噪声感知版）**：影响 agent 行为的变更必须带 paired 评测结果；门禁按 §6 执行；模型版本变更强制重测基线。
- **受控实验原则**：多智能体扩张、OTel 转正等「贵的架构升级」必须带对照组数据，无净增益即停。
- **任务级 DoD**：代码+测试+文档+（影响 agent 行为时）评测。
- **发布节奏**：semver；月度 minor；破坏性变更有升级指南与弃用期。
- **诚实呈现**：基准两档披露；降级报告明示缺失；校准报告明示窗口局限。

---

## 9. 决策门裁决记录（累计；M0 补录正式 ADR）

| # | 决策 | 裁决 | 备注 |
|---|------|------|------|
| 1 | 许可证 | **Apache 2.0** | 含专利授权 |
| 2 | Python 矩阵 | **3.11–3.13** | 维护者默认 |
| 3 | PyPI 包名 | **`bellwether-agent`** | `bellwether` 被占；CLI 仍为 `bellwether` |
| 4 | 插件密钥 | **env 为主 + keyring 可选** | 永不明文落盘 |
| 5 | LLM 定价表 | **内置+可覆盖** | 维护者默认 |
| 6 | 异步范围 | **A1 全面 async** | 同步库 to_thread 包装；M1 先落边界 |
| 7 | 美股专业源 | **M5 开工前专项对比**（Polygon vs FMP） | Tushare 档位同期定 |
| 8 | 电话会纪要 | **不内置**（插件生态期望） | v1.1 backlog 可见 |
| 9 | 黄金集规模 | **30×3 市场×2 档（quick/deep）起步** | 时点维度由 A0 随日历积累（C2b） |
| 10 | 公开基准姿态 | **只对比钉死的「裸 LLM+搜索」基线** | 能力矩阵替代点名对比 |
| 11 | MCP 时机 | **M5 随 GA**，过 E5 安全门禁 | API/TUI 仍为决策门 |
| 12 | 默认档位 | **analyze 快路径 / --deep 全编排** | B2 受控实验决定编排扩张 |
| ★13 | 战略承诺措辞（2026-07-16，评审后） | **采纳重述**：对外承诺=「最可审计的开源 US/CN/HK 股票研究 agent」；「全球顶尖」为内部北极星；基准两档披露 | 两评审共识方向 |
| ★14 | v1.0 砍减力度（2026-07-16，评审后） | **温和版**：✂B4/A8/A7b→v1.1；OTel→可选；PDF→stretch；文档站英文为主；TCK 最小化；**保留 B5/B6** | 全部为延后非删除 |

---

## 10. v1.1 Backlog（延后项，非删除）

B4 因子库+筛选器 · A8 宏观数据 · A7b 巨潮/HKEX 文件摄取（插件示范题材）· 电话会纪要（插件生态期望）· 内部人/机构持仓（EDGAR Forms 3/4/5、13F——「机构级报告」常规期待，评审建议）· PDF 导出转正 · OTel 转正（MCP/API 落地时）· 中文文档完整镜像 · Homebrew tap · REST API / TUI（决策门）

---

## 附录 A：v1 路线图（E0–E6）→ v2/v2.1 映射

| v1 阶段 | 归宿 |
|---------|------|
| E0 工程地基 | D1 |
| E1 可靠性 | D2（+LLM 侧降级） |
| E2 可观测 | D3（OTel→可选） |
| E3 性能并发 | D4（+per-provider 限流） |
| E4 数据源插件化 | A1+A2（TCK 最小化） |
| E5 测试与 CI/CD | D5 |
| E6 开源收尾 | F1–F5（文档英文为主） |

## 附录 B：v2.1 评审来源

- 评审方式：deep-reasoner 与 Codex（GPT-5.4）并行独立对抗评审，互不可见；原文全文见 [docs/reviews/](docs/reviews/)。
- 共识采纳：依赖重排（A7a/E3/E1）、A0 时间物理、证据 IR、评测可运营化（分层确定性/噪声门禁/钉死基线/评测分层）、B2 受控实验、双时间 PIT、安全风险扩充、KPI 防刷分、工期 ×2、一致性修复。
- 分歧裁决：战略承诺措辞（决策 13）、砍减力度（决策 14）。

---

*本文档为 v2.1 规划定稿。实施按里程碑推进；每里程碑开工时细化任务并回填 ADR。*
